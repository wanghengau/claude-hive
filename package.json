{
  "name": "claude-hive",
  "description": "Multi-window Claude Code manager with optional API recording",
  "workspaces": ["server", "web"],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"npm -w server run dev\" \"npm -w web run dev\"",
    "build": "npm -w web run build && npm -w server run build",
    "start": "npm -w server run dev",
    "test": "npm -w server test && npm -w web test"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
